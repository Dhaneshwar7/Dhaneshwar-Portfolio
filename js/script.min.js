$(window).on('load', function () {
	var a;
	new (class b {
		constructor() {
			(a = this),
				(a.emailChange = $('.email-change').html()),
				(a.singleBoardImage = $(
					'.three-board-section .advantages-images-wrapper .single-advantage-image'
				)),
				a.init();
		}
		init() {
			a.windowResize(),
				$(window).resize(a.windowResize),
				a.windowScroll(),
				$(window).scroll(a.windowScroll),
				$('.get-started').on('click', a.getStarted),
				$('.center-btn-area li').on('click', a.scrollToSection),
				$('.sticky-menu').on('click', a.openNav),
				$('#index_10_3 h1.page_title b').html('or drop us a message'),
				$('#main_form257 input#Name0').attr('placeholder', 'Your Name'),
				$('#main_form257 input#Contact1').attr('placeholder', 'Your Contact'),
				$('#main_form257 input#Email2').attr('placeholder', 'Your Email'),
				$('#main_form257 input#PhoneNumber3').attr(
					'placeholder',
					'Your Phone Number'
				),
				$('#main_form257 textarea').attr('placeholder', 'Leave Your Message'),
				$('#main_form257 textarea').attr('rows', '1');
		}
		windowScroll() {
			100 < $(window).scrollTop() && 768 < window.innerWidth
				? ($('.top-nav-sec').addClass('fixed-nav'),
				  $('.top-nav-wrapper').hasClass('clicked') &&
						a.openNav.call($('.sticky-menu')))
				: 0 == $(window).scrollTop() &&
				  $('.top-nav-sec').removeClass('fixed-nav');
		}
		windowResize() {
			a.singleBoardImage.css('height', a.singleBoardImage.first().innerWidth()),
				991 > window.innerWidth &&
					$('.top-nav-sec.fixed-nav') &&
					$('.top-nav-sec').removeClass('fixed-nav'),
				768 > window.innerWidth
					? $('.email-change').html('direct email')
					: $('.email-change').html(a.emailChange);
		}
		getStarted() {
			var a = $('.landing-effect-right').first().offset().top - 40;
			768 >= window.innerWidth && (a -= 80),
				$('body, html').stop().animate({ scrollTop: a }, 750);
		}
		scrollToSection() {
			var a,
				b = $(this).attr('data-target');
			'landing' == b
				? (a = 0)
				: ((a = $('#' + b)
						.first()
						.offset().top),
				  'features' == b
						? (a += 30)
						: 'portfolio' == b
						? (a += 180)
						: 'pricing' == b
						? (a += 150)
						: 'contact' == b && (a -= 90)),
				768 >= window.innerWidth &&
					('features' == b
						? (a -= 80)
						: 'portfolio' == b
						? (a -= 20)
						: 'pricing' == b
						? (a -= 80)
						: 'contact' == b && (a -= 90)),
				$('body, html').stop().animate({ scrollTop: a }, 1500),
				$('.top-nav-wrapper').hasClass('opened') &&
					$('.side-menu-burger i').trigger('click');
		}
		openNav() {
			$('.top-nav-sec.fixed-nav') &&
				($(this).parent().parent().parent().toggleClass('clicked'),
				$(this).parent().parent().parent().hasClass('clicked')
					? $(this).children('i').removeClass('fa-bars').addClass('fa-times')
					: $(this).children('i').removeClass('fa-times').addClass('fa-bars'));
		}
	})();
	var b;
	new (class a {
		constructor() {
			(b = this),
				b.currentA,
				b.cursorXInLaptop,
				b.cursorYInLaptop,
				(b.scrollSizeBefore = 0),
				768 < window.innerWidth && b.init();
		}
		init() {
			$('a.button-look').mouseenter(b.buttonLook),
				$('a').mouseleave(b.windowMouseLeave),
				$('.laptop-screen').mouseenter(b.laptopMouseMove),
				$('.laptop-screen').mousemove(b.laptopMouseMove),
				$('.laptop-screen').mouseleave(b.laptopMouseLeave),
				$(window).scroll(b.windowScroll);
		}
		buttonLook() {
			(b.currentA = this), $(b.currentA).bind('mousemove', b.buttonMove);
		}
		windowMouseLeave() {
			$(b.currentA).unbind('mousemove'),
				$(b.currentA).css('transform', 'translate(0px, 0px)');
		}
		laptopMouseMove(a) {
			(b.scrollSizeBefore = $(window).scrollTop()),
				(b.cursorXInLaptop = a.pageX - $(this).offset().left),
				(b.cursorYInLaptop = a.pageY - $(this).offset().top),
				$(this).css({ cursor: 'none' }),
				$(this).children('.laptop-cursor').addClass('show'),
				$(this)
					.children('.laptop-cursor')
					.css({ top: b.cursorYInLaptop, left: b.cursorXInLaptop });
		}
		laptopMouseLeave() {
			$(this).children('.laptop-cursor').removeClass('show');
		}
		windowScroll() {
			if ($('.laptop-cursor').hasClass('show')) {
				var a = $(window).scrollTop() - b.scrollSizeBefore;
				(b.cursorYInLaptop += a),
					$('.laptop-cursor').css({ top: b.cursorYInLaptop });
			}
			b.scrollSizeBefore = $(window).scrollTop();
		}
		buttonMove(a) {
			var c = a.pageX - $(b.currentA).offset().left,
				d = a.pageY - $(b.currentA).offset().top,
				e = Math.floor($(b.currentA).outerWidth() / 10),
				f = Math.floor($(b.currentA).outerHeight() / 10),
				g = 0,
				h = 0;
			c < Math.floor($(b.currentA).outerWidth() / 2)
				? ((g +=
						(-e * (Math.floor($(b.currentA).outerWidth() / 2) - c)) /
						Math.floor($(b.currentA).outerWidth() / 2)),
				  (g = Math.floor(g)))
				: ((g +=
						(e * (c - Math.floor($(b.currentA).outerWidth() / 2))) /
						Math.floor($(b.currentA).outerWidth() / 2)),
				  (g = Math.ceil(g))),
				d < Math.floor($(b.currentA).outerWidth() / 2)
					? ((h +=
							(-f * (Math.floor($(b.currentA).outerHeight() / 2) - d)) /
							Math.floor($(b.currentA).outerHeight() / 2)),
					  (h = Math.floor(h)))
					: ((h +=
							(f * (d - Math.floor($(b.currentA).outerHeight() / 2))) /
							Math.floor($(b.currentA).outerHeight() / 2)),
					  (h = Math.ceil(h))),
				$(this).css('transform', 'translate(' + g + 'px, ' + h + 'px)');
		}
	})();
	new (class a {
		constructor() {
			this.init();
		}
		init() {
			$('.side-menu-burger i').on('click', this.sideMenuClicked);
		}
		sideMenuClicked() {
			$('.top-nav-wrapper').toggleClass('opened'),
				$('.top-nav-wrapper').hasClass('opened')
					? ($(this).removeClass('fa-bars').addClass('fa-times'),
					  $('.top-nav-wrapper .top-nav .mobile-bottom-contact').css(
							'transition',
							'.1s .4s'
					  ))
					: ($(this).removeClass('fa-times').addClass('fa-bars'),
					  $('.top-nav-wrapper .top-nav .mobile-bottom-contact').css(
							'transition',
							'.1s'
					  ));
		}
	})();
	var c;
	new (class a {
		constructor() {
			(c = this),
				c.interval,
				(c.flag = !0),
				(c.windowScrollTop = 0),
				(c.leftWidth = -$('.text-running-way-left .single-services-list')
					.first()
					.outerWidth(!0)),
				(c.leftWidthMove = -$('.text-running-way-left .single-services-list')
					.first()
					.outerWidth(!0)),
				(c.leftTextRow = $('.text-running-way-left .text-row')),
				(c.rightWidth = -$('.text-running-way-right .single-services-list')
					.first()
					.outerWidth(!0)),
				(c.rightWidthMove =
					2 *
						-$('.text-running-way-right .single-services-list')
							.first()
							.outerWidth(!0) +
					window.innerWidth),
				(c.rightTextRow = $('.text-running-way-right .text-row')),
				c.init();
		}
		init() {
			c.leftTextRow.append(
				$('.text-running-way-left .single-services-list').first().clone(!0, !0)
			),
				c.leftTextRow.append(
					$('.text-running-way-left .single-services-list')
						.first()
						.clone(!0, !0)
				),
				c.leftTextRow.css('transform', 'translateX(' + c.leftWidth + 'px)'),
				c.rightTextRow.append(
					$('.text-running-way-right .single-services-list')
						.first()
						.clone(!0, !0)
				),
				c.rightTextRow.append(
					$('.text-running-way-right .single-services-list')
						.first()
						.clone(!0, !0)
				),
				c.rightTextRow.css(
					'transform',
					'translateX(' + (2 * c.rightWidth + window.innerWidth) + 'px)'
				),
				$(window).scroll(c.windowScroll),
				c.windowScroll();
		}
		windowScroll() {
			var a;
			$(window).scrollTop() > c.windowScrollTop
				? (a = !0)
				: $(window).scrollTop() < c.windowScrollTop && (a = !1),
				(c.windowScrollTop = $(window).scrollTop()),
				$(window).scrollTop() + window.innerHeight >=
					$('.text-running-way-sec').first().offset().top &&
					$(window).scrollTop() <=
						$('.text-running-way-sec').first().offset().top +
							$('.text-running-way-sec').innerHeight() &&
					c.flag &&
					((c.flag = !1),
					a
						? (clearInterval(c.interval),
						  (c.leftWidthMove -= 200),
						  c.leftTextRow.css({
								transition: '.5s',
								transform: 'translateX(' + c.leftWidthMove + 'px)',
						  }),
						  (c.rightWidthMove += 200),
						  c.rightTextRow.css({
								transition: '.5s',
								transform: 'translateX(' + c.rightWidthMove + 'px)',
						  }),
						  setTimeout(c.continueMove, 500))
						: !a &&
						  (clearInterval(c.interval),
						  0 < c.leftWidthMove + 200 &&
						  c.rightWidthMove - 200 < 3 * c.rightWidth + window.innerWidth
								? (c.leftTextRow.css('transition', 'unset'),
								  (c.leftWidthMove += c.leftWidth),
								  c.leftTextRow.css(
										'transform',
										'translateX(' + c.leftWidthMove + 'px)'
								  ),
								  c.rightTextRow.css('transition', 'unset'),
								  (c.rightWidthMove -= c.rightWidth),
								  c.rightTextRow.css(
										'transform',
										'translateX(' + c.rightWidthMove + 'px)'
								  ),
								  setTimeout(function () {
										(c.leftWidthMove += 200),
											c.leftTextRow.css({
												transition: '.5s',
												transform: 'translateX(' + c.leftWidthMove + 'px)',
											}),
											(c.rightWidthMove -= 200),
											c.rightTextRow.css({
												transition: '.5s',
												transform: 'translateX(' + c.rightWidthMove + 'px)',
											}),
											setTimeout(c.continueMove, 500);
								  }, 10))
								: 0 < c.leftWidthMove + 200
								? ((c.rightWidthMove -= 200),
								  c.rightTextRow.css({
										transition: '.5s',
										transform: 'translateX(' + c.rightWidthMove + 'px)',
								  }),
								  c.leftTextRow.css('transition', 'unset'),
								  (c.leftWidthMove += c.leftWidth),
								  c.leftTextRow.css(
										'transform',
										'translateX(' + c.leftWidthMove + 'px)'
								  ),
								  setTimeout(function () {
										(c.leftWidthMove += 200),
											c.leftTextRow.css({
												transition: '.5s',
												transform: 'translateX(' + c.leftWidthMove + 'px)',
											}),
											setTimeout(c.continueMove, 500);
								  }, 10))
								: c.rightWidthMove - 200 < 3 * c.rightWidth + window.innerWidth
								? ((c.leftWidthMove += 200),
								  c.leftTextRow.css({
										transition: '.5s',
										transform: 'translateX(' + c.leftWidthMove + 'px)',
								  }),
								  c.rightTextRow.css('transition', 'unset'),
								  (c.rightWidthMove -= c.rightWidth),
								  c.rightTextRow.css(
										'transform',
										'translateX(' + c.rightWidthMove + 'px)'
								  ),
								  setTimeout(function () {
										(c.rightWidthMove -= 200),
											c.rightTextRow.css({
												transition: '.5s',
												transform: 'translateX(' + c.rightWidthMove + 'px)',
											}),
											setTimeout(c.continueMove, 500);
								  }, 10))
								: ((c.leftWidthMove += 200),
								  c.leftTextRow.css({
										transition: '.5s',
										transform: 'translateX(' + c.leftWidthMove + 'px)',
								  }),
								  (c.rightWidthMove -= 200),
								  c.rightTextRow.css({
										transition: '.5s',
										transform: 'translateX(' + c.rightWidthMove + 'px)',
								  }),
								  setTimeout(c.continueMove, 500))));
		}
		continueMove() {
			(c.flag = !0),
				c.leftTextRow.css('transition', 'unset'),
				c.rightTextRow.css('transition', 'unset'),
				(c.interval = setInterval(function () {
					if (c.leftWidthMove < 2 * c.leftWidth) {
						var a = 2 * c.leftWidth - c.leftWidthMove;
						c.leftWidthMove = c.leftWidth - a;
					}
					if (
						(c.leftTextRow.css(
							'transform',
							'translateX(' + c.leftWidthMove-- + 'px)'
						),
						c.rightWidthMove > c.rightWidth + window.innerWidth)
					) {
						var a = c.rightWidthMove - (c.rightWidth + window.innerWidth);
						c.rightWidthMove = 2 * c.rightWidth + window.innerWidth + a;
					}
					c.rightTextRow.css(
						'transform',
						'translateX(' + c.rightWidthMove++ + 'px)'
					);
				}, 10));
		}
	})();
	var d;
	new (class a {
		constructor() {
			(d = this),
				(d.stickyHeight = $('.sticky-height').first()),
				1e3 < window.innerWidth && d.init();
		}
		init() {
			$(window).scroll(d.windowScroll),
				d.windowScroll(),
				$(window).resize(d.windowRResize),
				d.windowScroll();
		}
		windowScroll() {
			if (1e3 < window.innerWidth)
				if ($(window).scrollTop() <= d.stickyHeight.offset().top)
					$('.single-board').css('transform', 'translate(0px, 0px)');
				else if (
					$(window).scrollTop() >= d.stickyHeight.offset().top &&
					$(window).scrollTop() <=
						d.stickyHeight.offset().top + 1.5 * window.innerHeight
				) {
					var a = -($(window).scrollTop() - d.stickyHeight.offset().top);
					(a = 1 * (a / 1.5)),
						$('.single-board')
							.eq(0)
							.css('transform', 'translate(0px, ' + a + 'px)'),
						$('.single-board').eq(1).css('transform', 'translate(0px, 0px)');
				} else if (
					$(window).scrollTop() >=
						d.stickyHeight.offset().top + 1.5 * window.innerHeight &&
					$(window).scrollTop() <=
						d.stickyHeight.offset().top + 3 * window.innerHeight
				) {
					$('.single-board')
						.eq(0)
						.css('transform', 'translate(0px, ' + -window.innerHeight + 'px)');
					var a = -(
						$(window).scrollTop() -
						(d.stickyHeight.offset().top + 1.5 * window.innerHeight)
					);
					(a = 1 * (a / 1.5)),
						$('.single-board')
							.eq(1)
							.css('transform', 'translate(0px, ' + a + 'px)');
				} else if (
					$(window).scrollTop() >=
						d.stickyHeight.offset().top + 3 * window.innerHeight &&
					$(window).scrollTop() <=
						d.stickyHeight.offset().top + 4.5 * window.innerHeight
				) {
					$('.single-board')
						.eq(0)
						.css('transform', 'translate(0px, ' + -window.innerHeight + 'px)'),
						$('.single-board')
							.eq(1)
							.css(
								'transform',
								'translate(0px, ' + -window.innerHeight + 'px)'
							);
					var a = -(
						$(window).scrollTop() -
						(d.stickyHeight.offset().top + 3 * window.innerHeight)
					);
					a = 1 * (a / 1.5);
				}
		}
	})();
	var e;
	new (class a {
		constructor() {
			(e = this),
				e.portfolioWidth,
				e.transformNumber,
				(e.scrollContainer = $('.horizontal-scroll-container').first()),
				(e.scrollPortfolio = $('.portfolio-section').first()),
				(e.scrollPricing = $('.pricing-section').first()),
				e.init();
		}
		init() {
			$(window).scroll(e.windowScroll),
				e.windowScroll(),
				$(window).resize(e.windowResize),
				e.windowResize();
		}
		windowScroll() {
			if (
				(e.changeBg(),
				$(window).scrollTop() + window.innerHeight >=
					e.scrollContainer.offset().top &&
					$(window).scrollTop() + window.innerHeight <=
						e.scrollContainer.offset().top + e.scrollContainer.outerHeight(!0))
			) {
				var a =
						$(window).scrollTop() +
						window.innerHeight -
						e.scrollContainer.offset().top,
					b = Math.ceil(e.transformNumber - a);
				$('.horizontal-scroll-container .all-portfolio').css(
					'transform',
					'translate(' + b + 'px, -50%)'
				);
			} else $(window).scrollTop() + window.innerHeight <= e.scrollContainer.offset().top ? $('.horizontal-scroll-container .all-portfolio').css('transform', 'translate(' + e.transformNumber + 'px, -50%)') : $(window).scrollTop() + window.innerHeight >= e.scrollContainer.offset().top + e.scrollContainer.outerHeight(!0) && (600 < window.innerWidth ? $('.horizontal-scroll-container .all-portfolio').css('transform', 'translate(' + (e.transformNumber - e.portfolioWidth + Math.floor(0.25 * window.innerWidth)) + 'px, -50%)') : $('.horizontal-scroll-container .all-portfolio').css('transform', 'translate(' + (e.transformNumber - e.portfolioWidth) + 'px, -50%)'));
		}
		windowResize() {
			600 < window.innerWidth
				? ((e.portfolioWidth = $('.horizontal-scroll-container .all-portfolio')
						.first()
						.outerWidth(!0)),
				  e.scrollContainer.css(
						'height',
						e.portfolioWidth - Math.floor(0.25 * window.innerWidth) + 'px'
				  ),
				  (e.transformNumber = Math.floor(0.7 * window.innerWidth)))
				: ($('.horizontal-scroll-container .show-all-portfolio').css(
						'margin-right',
						(window.innerWidth - 260) / 2 + 'px'
				  ),
				  (e.portfolioWidth =
						$('.horizontal-scroll-container .all-portfolio')
							.first()
							.outerWidth(!0) + 300),
				  e.scrollContainer.css('height', e.portfolioWidth + 'px'),
				  (e.transformNumber = Math.floor(window.innerWidth + 300))),
				$('.horizontal-scroll-container .all-portfolio').css(
					'transform',
					'translate(' + e.transformNumber + 'px, -50%)'
				);
		}
		changeBg() {
			$(window).scrollTop() - 100 >= e.scrollPortfolio.offset().top &&
			$(window).scrollTop() + window.innerHeight / 3 <=
				e.scrollPricing.offset().top
				? ($('body').css('backgroundColor', '#222'),
				  $('body').addClass('black'))
				: $(window).scrollTop() + window.innerHeight / 3 >=
				  e.scrollPricing.offset().top
				? ($('body').css('backgroundColor', '#fff'),
				  $('body').removeClass('black'))
				: $(window).scrollTop() - 100 <= e.scrollPortfolio.offset().top &&
				  ($('body').css('backgroundColor', '#fff'),
				  $('body').removeClass('black'));
		}
	})();
	var f;
	new (class a {
		constructor() {
			(f = this), (f.fadeBox = $('.fade-up-box')), f.setting(), f.init();
		}
		setting() {
			$.each($('.landing-fade-up-box .landing-fade-up-item'), function (a, b) {
				$(b).css('transitionDelay', (a / 3.5).toFixed(2) + 's');
			}),
				$.each(f.fadeBox, function (a, b) {
					$.each($(b).children('.fade-up-item'), function (a, b) {
						$(b).css('transitionDelay', (a / 3.5).toFixed(2) + 's');
					});
				});
		}
		init() {
			$(window).scroll(f.windowScroll), f.windowScroll();
		}
		windowScroll() {
			$.each($('.landing-fade-up-box:not(.show)'), function (a, b) {
				120 > $(window).scrollTop()
					? $(b).addClass('show')
					: ($(b).addClass('show'),
					  $.each(
							$('.landing-fade-up-box .landing-fade-up-item'),
							function (a, b) {
								$(b).css('transition', '0s');
							}
					  ));
			}),
				$.each($('.fade-up-box:not(.show)'), function (a, b) {
					$(window).scrollTop() + 3 * (window.innerHeight / 4) >=
						$(b).offset().top &&
						($(b).removeClass('no-duration'), $(b).addClass('show'));
				}),
				$.each($('.fade-up-box'), function (a, b) {
					$(window).scrollTop() + window.innerHeight <= $(b).offset().top &&
						($(b).addClass('no-duration'), $(b).removeClass('show'));
				}),
				$.each($('.single-advantage-image:not(.show)'), function (a, b) {
					$(window).scrollTop() + 3 * (window.innerHeight / 4) >=
						$(b).offset().top &&
						($(b).removeClass('no-duration'), $(b).addClass('show'));
				}),
				$.each($('.single-advantage-image'), function (a, b) {
					$(window).scrollTop() + window.innerHeight <= $(b).offset().top &&
						($(b).addClass('no-duration'), $(b).removeClass('show'));
				});
		}
	})();
	var g;
	new (class a {
		constructor() {
			(g = this), (g.flagLaptop = !0), (g.flagPhone = !0), g.init();
		}
		init() {
			$('.laptop-wrapper .select-card').on('click', g.cardClicked),
				$('.phone-wrapper .select-card').on('click', g.phoneCardClicked),
				$('.laptop-wrapper .back-button').on('click', g.backClicked),
				$('.phone-wrapper .back-button').on('click', g.phoneBackClicked);
		}
		cardClicked() {
			if (g.flagLaptop) {
				g.flagLaptop = !1;
				var a = $(this).attr('data-selection');
				$(this).parent().parent().css('left', '-110%'),
					$('.laptop-wrapper .select-result')
						.eq(parseInt(a) - 1)
						.css('left', 'calc(0% + 15px)');
			}
		}
		phoneCardClicked() {
			if (g.flagPhone) {
				g.flagPhone = !1;
				var a = $(this).attr('data-selection');
				$(this).parent().parent().css('left', '-110%'),
					$('.phone-wrapper .select-result')
						.eq(parseInt(a) - 1)
						.css('left', 'calc(0% + 15px)');
			}
		}
		backClicked() {
			(g.flagLaptop = !0),
				$(this).parent().parent().css('left', 'calc(100% + 15px)'),
				$('.laptop-wrapper .selections').css('left', '0%');
		}
		phoneBackClicked() {
			(g.flagPhone = !0),
				$(this).parent().parent().css('left', 'calc(100% + 15px)'),
				$('.phone-wrapper .selections').css('left', '0%');
		}
	})();
	var h;
	new (class a {
		constructor() {
			(h = this),
				(h.scrollProcess = $('.process-section').first()),
				(h.top = h.scrollProcess.offset().top),
				(h.height = h.scrollProcess.outerHeight()),
				h.init();
		}
		init() {
			$(window).scroll(h.windowScroll),
				h.windowScroll(),
				$(window).resize(h.windowResize),
				h.windowResize(),
				$('.step-by-step-button').on('click', h.scrollToStep),
				$('.single-process .next-step').on('click', h.scrollStepByStep);
		}
		windowScroll() {
			var a = $(window).scrollTop() + window.innerHeight / 2;
			768 < window.innerWidth
				? a >= h.top && a < h.top + 0.1 * h.height
					? h.controlClass(!0, 'Explore my<br>education<br>journey now', !1, !0)
					: a >= h.top + 0.1 * h.height && a < h.top + 0.34 * h.height
					? (h.controlClass(!1, !1, '1'),
					  $('.process-section .process-1')
							.addClass('current')
							.siblings()
							.removeClass('current'))
					: a >= h.top + 0.34 * h.height && a < h.top + 0.5 * h.height
					? (h.controlClass(!1, !1, '2'),
					  $('.process-section .process-2')
							.addClass('current')
							.siblings()
							.removeClass('current'))
					: a >= h.top + 0.5 * h.height && a < h.top + 0.66 * h.height
					? (h.controlClass(!1, !1, '3'),
					  $('.process-section .process-3')
							.addClass('current')
							.siblings()
							.removeClass('current'))
					: a >= h.top + 0.66 * h.height && a < h.top + 0.9 * h.height
					? (h.controlClass(!1, !1, '4'),
					  $('.process-section .process-4')
							.addClass('current')
							.siblings()
							.removeClass('current'))
					: a >= h.top + 0.9 * h.height &&
					  h.controlClass(
							!0,
							'The learning process<br>continues until<br> the day you die.',
							!1
					  )
				: a >= h.top + 0.05 * h.height && a < h.top + 0.315 * h.height
				? (h.controlClass(!1, !1, '1'),
				  $('.process-section .process-1')
						.addClass('current')
						.siblings()
						.removeClass('current'))
				: a >= h.top + 0.315 * h.height && a < h.top + 0.5 * h.height
				? (h.controlClass(!1, !1, '2'),
				  $('.process-section .process-2')
						.addClass('current')
						.siblings()
						.removeClass('current'))
				: a >= h.top + 0.5 * h.height && a < h.top + 0.685 * h.height
				? (h.controlClass(!1, !1, '3'),
				  $('.process-section .process-3')
						.addClass('current')
						.siblings()
						.removeClass('current'))
				: a >= h.top + 0.685 * h.height &&
				  a < h.top + 0.95 * h.height &&
				  (h.controlClass(!1, !1, '4'),
				  $('.process-section .process-4')
						.addClass('current')
						.siblings()
						.removeClass('current'));
		}
		windowResize() {
			(h.scrollProcess = $('.process-section').first()),
				(h.top = h.scrollProcess.offset().top),
				(h.height = h.scrollProcess.outerHeight());
		}
		controlClass(a, b, c, d) {
			a
				? ($('.process-section .circle').removeClass('small'),
				  $('.process-section .single-process').removeClass('current'))
				: $('.process-section .circle').addClass('small'),
				b &&
					$('.process-section .circle')
						.children('.circle-content')
						.children('p.text')
						.html(b),
				c &&
					$('.process-section .circle')
						.children('.circle-content')
						.children('p.number')
						.html(c),
				d
					? $('.process-section .circle').addClass('down-btn')
					: $('.process-section .circle').removeClass('down-btn');
		}
		scrollToStep() {
			var a = h.top + 0.26 * h.height - window.innerHeight / 2;
			$('body, html').stop().animate({ scrollTop: a }, 750);
		}
		scrollStepByStep() {
			var a = $(this).attr('data-target');
			if (768 < window.innerWidth) var b = 0.261 + 0.16 * a;
			else var b = 0.173 + 0.193 * a;
			var c = h.top + h.height * b - window.innerHeight / 2;
			$('body, html').stop().animate({ scrollTop: c }, 750);
		}
	})();
	var i;
	new (class a {
		constructor() {
			(i = this), this.init(portfolio_data);
		}
		init(a) {
			a.forEach(a => {
				const b = document.getElementById(a.id),
					c = lightGallery(b, {
						plugins: [lgZoom, lgAutoplay, lgFullscreen, lgThumbnail, lgVideo],
						speed: 100,
						download: false,
						dynamic: !0,
						dynamicEl: a.images,
					});
				b.addEventListener('click', function () {
					c.openGallery(0);
				});
			});
		}
	})();
	const buttons = document.querySelectorAll('.slbutton');
	buttons.forEach(button => {
		console.log(button);
		let target = button.querySelector('.target');
		console.log(target);
		function handleMove(e) {
			const x = -180 + (e.pageX - button.offsetLeft - 300 / 2) / 4;
			console.log(x);
			const y = -145 + (e.pageY - button.offsetTop - 300 / 2) / 10;
			console.log(y);


			target.style.setProperty('--x', `${x}px`);
			target.style.setProperty('--y', `${y}px`);
		}
		button.addEventListener('mousemove', e => {
			handleMove(e);
			console.log('djkjf');
		});
		button.addEventListener('touchmove', e => {
			handleMove(e.changedTouches[0]);
		});
	});
});
